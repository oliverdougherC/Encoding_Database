# Hosted Windows input proof and separate Close failure

[CI 35488231577 / Windows job 106018407921](https://github.com/oliverdougherC/Encoding_Database/actions/runs/35488231577/job/106018407921) built head `b98d3b26c1c906eed992cce3e0268fa2ff8a9993` through verified merge `1f6a8c3dbd5a531d66aa76f60b4763de3a98ff9a`. Its own GUI binary SHA256 is `b7ead853e363de313629bed5a0ef86862f5db469547d676d8d54418d0af63657`; console SHA256 is `8fee02478b2f3c8a9f0d17571b694e44d5e7bad6d5430b8041f88e3db055f742`. These are distinct build identities from the earlier b3 run.

The normal native keyboard input correction executed successfully on this hosted runner. All six Start/Stop input receipts report four inserted events, forty-byte INPUT, matching focused-child/target process, exact foreground before and after, and Caps Lock off. Preparation Stop, completed Single run and measured Stop each reached a clean process exit and no owned survivors. This establishes actual input/phase progress here; it does not prove the physical desktop received input.

Close remained **failed**, not accepted. After the active-run window-close request, the native Exit dialog visibly appeared with Yes focused. The harness failed before any `before-action-Yes` capture or native-click event. Cleanup then failed with `.Kill(): Access is denied` and overwrote the primary exception. The completed GitHub job log contains only that cleanup error. Therefore the exact original Close exception and denied cleanup target cannot be reconstructed. The screenshots/events bound the failure interval to confirmation discovery, but do not establish a client defect or a specific UIA exception. The failed GUI phase has no completed cleanup proof.

The follow-up operator patch preserves primary error/type/stage before capture/cleanup, records cleanup failures separately, continues only creation-bound owned-process cleanup, and polls the same unique owned native Exit/Yes predicate used for invocation. It retains the confirmation, focus, click, cancellation and artifact-retention requirements. That patch does not retroactively pass this run.

The independent console seven-clip step **passed** at 2026-09-20 04:33:14 UTC: exit 0, seven warmups and fourteen measured attempts across all seven canonical clips, embedded helper attestations, retained SHA checks, no forced cleanup and no owned survivors. This is virtualized Windows software acceptance only. Its verifier receipt is retained here; no new local media download/hash audit was performed during the concurrent Mac timing allocation.
