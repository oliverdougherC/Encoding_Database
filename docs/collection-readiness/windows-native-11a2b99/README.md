# Windows native acceptance: interrupted evidence retention

[Run 34815842127](https://github.com/oliverdougherC/Encoding_Database/actions/runs/34815842127/job/103886217990) tested PR head `11a2b99544058d11561210a5b78f3eeb55320388` through merge commit `f2528dc3c87c0222e681b77837e15dcb1966861b`. GitHub step metadata records native build success at 2026-09-14 07:13:27 UTC, followed by GUI acceptance failure at 07:43:30 UTC. The seven-clip console step then started, but the outer job was cancelled. The check annotation explicitly identifies the 60-minute maximum job duration; it does not identify an account limit.

The job log endpoint returns “log not found”, the downloaded run-log archive has no Windows entries, and the artifact inventory contains only Linux/macOS candidates and clean-deployment evidence. No GUI phase, screenshot, receipt, complete seven-clip result, packaged binary, or embedded-helper attestation can be recovered from this run. Accordingly it does not establish GUI, cancellation, seven-clip, or physical Windows/GPU acceptance. The GUI failure's precise cause remains unknown; the 30-minute duration alone is insufficient to diagnose it.

The workflow previously allowed a 60-minute job to contain a native build (13 minutes observed) and two independent 31-minute acceptance steps, with all evidence uploads last. The resumed workflow allows 90 minutes for that bounded sequence, retaining each mode's small diagnostics immediately after its acceptance step. Individual acceptance and measurement deadlines are unchanged. The harness also checkpoints its receipt and emits progress events to the runner log. The next run must still execute all four GUI phases and the complete seven-clip console campaign, and its screenshots require human/model visual inspection before a GUI acceptance claim.

Validation here: six existing fail-closed evidence-verifier tests pass; `git diff --check` passes. This macOS host has no PowerShell executable, so the changed PowerShell harness still requires execution on the Windows runner. No acceptance gate was relaxed.
