# Preserved observations from the affected sampler

Original cells 3–17 completed before the confirmed background-sampler defect was reported. The scheduler was stopped while cell 17 finished naturally, then terminated after that child reached zombie state. Cell 18 never started. Its actual exit status was read from the retained kernel wait status; the boundary receipt records the method. No timed encode was interrupted.

The first two cells plus this phase contain 17 cells and 57 retained attempts. Fourteen groups passed timing spread and three failed it. **All 17 are exploratory and ineligible for final calibration**, because the affected sampler's historical background flags cannot be reconstructed. Manual exclusive allocation cannot substitute for those observations.

The audited JSON verifies all retained artifact hashes, exact 720-frame counts and unchanged measured timing/FPS tuples. It preserves original reported validity fields as evidence of what the affected source produced, not as corrected facts. No authoritative metric analysis, database import or PL activation was performed.

A fresh execution revision must use the corrected sampler, the same finite source/recipe choices and new campaign seeds/output paths. Original attempts, failures and receipts remain intact.
