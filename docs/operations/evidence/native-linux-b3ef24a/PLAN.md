# Prepared b3ef24a Linux acceptance

Status: **not executed**. The capacity lane owns P910. This local packet does not
authorize host work before its terminal/drained handback and the parent's allocation.

The complete source, executable hash, paths, seed derivation, recipes and budgets
are frozen in [plan.json](plan.json). The seeds are deterministic functions of this
candidate commit and phase, declared before observing any b3 benchmark outcome.
They are distinct from the earlier939 software seed. No previous record is renamed,
reclassified as b3 output, merged into these queues or used to replace an outcome.

1. Check the candidate source, server image, worker concurrency0 and preserved
   physical-host state. Acquire its existing outer measurement lock nonblocking.
2. Create the new case and Japanese/space working path exclusively, then copy the
   b3 executable outside its build checkout and verify its exact package hash.
3. Build a task-owned trust bundle from the existing public roots and candidate
   public certificate. Scrub runtime/helper/loader overrides. Keep physical state
   unchanged; create a fresh suite cache. Do not use a tracer.
4. Run the binary's help and quick `libx264/fast/CRF24` no-submit smoke, using the
   existing owned-process supervisor and its separate acquisition/measurement
   limits. Put all logs and runtime receipts under this new case, leaving prior
   candidate scratch evidence and all939 case directories untouched. Prove frozen
   `_MEI` helper paths and expected hashes. Retain any failed preparation as failure.
5. Run the full-seven software and NVENC recipes exactly once each, with their
   separately declared seeds and only the protocol's own adaptive repeats. Keep
   the shared lock across these phases. Preserve suspect/invalid/unstable outcomes.
6. Before publication, capture immutable ledgers for the completed campaigns and
   audit exact frames, commands, native architecture, selectedGPU/driver, finite
   fresh CPU markers and NVML samples. Report missing optional telemetry honestly.
7. Verify owned-process drain, unchanged executable/physical identity and lock
   release, then hand the host back. No upload or quality worker starts automatically.

Next phases require the parent's coordination: native cancellation/storage/replay
fault acceptance where still required, then all-OS publication faults and actual
authoritative analysis/object/DB reconciliation. The earlier runbook's loopback TLS
fault proxy and immutable ledger remain available; injected transport pressure
must stay distinct from real server admission backpressure. Preserve raw private
logs on the host and publish only token-free evidence.
