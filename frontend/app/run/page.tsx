import styles from "./page.module.css";

export default function RunPage() {
  return <div className={`page ${styles.page}`}><header><p className={styles.kicker}>Contribute results</p><h1>Run a benchmark</h1><p>The benchmark client runs on your machine. Publication is opt-in, queue-backed, and gated by one-time durable consent.</p></header><div className={styles.grid}><main className={styles.panel}><ol><li><span>1</span><div><h2>Install the client</h2><p>Download the latest supported client from GitHub Releases.</p><a className="btn btn-primary" href="https://github.com/oliverdougherC/Encoding_Database/releases" target="_blank" rel="noreferrer">Open releases</a></div></li><li><span>2</span><div><h2>Run the canonical benchmark</h2><p>The client records canonical workload, recipe, performance, quality, and environment metadata. Do not run arbitrary or personal media through the publish path.</p><pre>encodingdb benchmark</pre></div></li><li><span>3</span><div><h2>Review and queue locally</h2><p>Interactive runs can ask once for durable publish consent. Noninteractive publication requires <code>--submit</code>; <code>--no-submit</code> keeps results local.</p><pre>encodingdb benchmark --submit</pre></div></li><li><span>4</span><div><h2>Inspect or clean the queue</h2><p>The publish queue is persistent. Check status or remove stale items explicitly before retrying.</p><pre>{`encodingdb --queue-status\nencodingdb --queue-cleanup`}</pre></div></li></ol></main><aside className={styles.aside}><p className={styles.kicker}>What is retained</p><ul><li>Canonical encoded artifact needed for authoritative verification and retention review</li><li>Hardware identifiers and environment fingerprints</li><li>Benchmark protocol, recipe, FFmpeg, driver, and client versions</li><li>Performance, quality, bitrate, and evidence metadata</li><li>Validation and queue state metadata</li></ul><p className={styles.callout}><strong>Privacy note.</strong> Do not include credentials, arbitrary footage, or personally sensitive media in benchmark runs or notes.</p></aside></div></div>;
}
