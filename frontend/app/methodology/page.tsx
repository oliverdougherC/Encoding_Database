import styles from "./page.module.css";
export default function MethodologyPage(){return <article className={`page ${styles.page}`}><header><p className={styles.kicker}>Reference</p><h1>Methodology</h1><p>How EncodingDB records, validates, and presents reproducible benchmark data.</p></header><Section title="Canonical workload"><p>Benchmark submissions record the input checksum, encoder configuration, resolution, and FFmpeg version when available. Results with different workloads or configurations should not be treated as directly equivalent.</p></Section><Section title="Metrics collected"><p>Where supplied by the benchmark client, the database retains elapsed time, FPS, output size, VMAF, SSIM, PSNR, power, memory, thermal telemetry, hardware identity, driver information, and software versions.</p></Section><Section title="Validation"><p>Only accepted benchmark rows are shown in Browse. Missing telemetry is displayed as not reported rather than inferred. Sample count remains visible so readers can assess the amount of supporting data.</p></Section><Section title="Aggregation"><p>Rows may represent compatible public submissions aggregated by hardware and encoding configuration. Run details identify the sample count and retained provenance fields.</p></Section><Section title="Privacy"><p>The client runs benchmarks locally. Publishing is an explicit submission action; benchmark metadata is not presented as uploaded until that action is taken.</p></Section></article>};function Section({title,children}:{title:string;children:React.ReactNode}){return <section><h2>{title}</h2>{children}</section>}
