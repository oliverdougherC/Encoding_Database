import styles from "./page.module.css";

export default function MethodologyPage(){return <div className={`page ${styles.wrap}`}><header><p className={styles.kicker}>Data provenance</p><h1>Methodology</h1><p>The methodology behind a benchmark database should be unusually clear. Every public number needs an understandable path back to its workload, command, environment, aggregation rules, and validation state.</p></header><article className={styles.article}><Section title="Canonical workload"><p>Benchmark submissions record the input checksum, encoder configuration, resolution, and FFmpeg version when available. Results with different workloads or configurations should not be treated as directly equivalent.</p><pre>input: canonical EncodingDB workload{`\n`}checksum: recorded with each attributable submission{`\n`}resolution: reported by the benchmark client{`\n`}encoder: normalized implementation and preset</pre></Section><Section title="Collected metrics"><p>Where supplied by the benchmark client, EncodingDB retains elapsed time, FPS, output size, VMAF, SSIM, PSNR, power, memory, thermal telemetry, hardware identity, driver information, and software versions.</p></Section><Section title="Validation"><ul><li>Input provenance must satisfy the accepted workload rules.</li><li>Encoder parameters must parse into a normalized configuration.</li><li>Output duration and frame counts must remain internally consistent.</li><li>Impossible or contradictory telemetry is rejected.</li><li>Missing telemetry is shown as not reported instead of inferred.</li></ul></Section><Section title="Aggregation"><p>Browse rows aggregate accepted submissions sharing the same benchmark configuration. Aggregate metrics are averages, and sample count remains visible so readers can judge how much evidence supports each row. Submission-specific versions, timing, notes, and input provenance are not attributed to an aggregate row.</p></Section><Section title="Privacy and publication"><p>The client runs benchmarks locally. Hardware and performance metadata is published only after an explicit submission action; local execution is not presented as a public upload.</p></Section></article></div>}

function Section({title,children}:{title:string;children:React.ReactNode}){return <section><h2>{title}</h2>{children}</section>}
