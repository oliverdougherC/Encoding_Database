# Validation-only authored screen source

This new30-second deterministic replay derives from the repository's CC0 Atlas
screen source. It contains different fictional receipts, log text and events:
scrolling, filtering, a detail drawer, documentation and incremental search.
It does not loop, stretch or re-encode the frozen10-second source capture.

All new authored HTML/replay content is CC0-1.0, like its source. IBM Plex Mono
remains SIL-OFL-1.1; the existing checked-in font and license are reused by relative
path. No external network, real dataset or personal information is included.
The source is browser-rendered screen content, not natural camera footage.
